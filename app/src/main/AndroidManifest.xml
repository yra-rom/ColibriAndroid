<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.plague.app090816registration">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LogInAndRegistration.activities.LogInActivity"/>
        <activity android:name=".LogInAndRegistration.activities.RegistrationActivity"/>
        <activity android:name=".Messaging.activities.MessagesActivity" android:theme="@style/MessageTheme"/>
        <activity android:name=".FriendInfoActivity" android:theme="@style/MessageTheme"/>
        <activity android:name=".Tabs.TabsActivity" android:theme="@style/MyMaterialTheme.Base"/>

    </application>

</manifest>